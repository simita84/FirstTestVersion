
<html>
<body>

<div class="hero-unit">

<div>
  <h2>Add Guest</h2>

  <%= form_for(:invitee, :url => {:action => 'create', :event_id => @event.id}) do |f| %>
  
  <%= render(:partial => "form", :locals => {:f => f}) %>
    
    <div class="btn">
      <%= submit_tag("Add Guest") %>
    </div>
	<%= link_to("Cancel", {:action => 'index', :event_id => @event.id},:class=>"btn") %>

   
  <% end %>
</div>
<%=flash[:notice]%>

</body>
</html>