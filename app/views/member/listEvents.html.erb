<html>
 

<body>
	
	
<div class="hero-unit">

<div class="btn">
<%= link_to("Back", {:controller => 'members', :action => 'index'}) %>
	</div>  



	<div class="category list">
	  <h2>Upcoming Events</h2>
 
	 <div><%= pluralize(@events.size, 'Event') %> found </div>

	  <table class="table">
	    <tr class="header">
	      <th>Event Name</th>
	      <th>Event Address</th>
	      <th>When </th>
	      <th>Duration</th>
	      <th>Actions</th>
	    </tr>
	    <% @events.each do |event| %>
	    
	      <td><%=raw event.name %></td>
	      <td><%= event.address %></td>
	      <td><%= event.datetime %></td>
	     <td><%= event.duration %></td>
	     <td>
<%= link_to("Guests List",  {:controller => 'invitees',:action => 'index', :event_id => event.id }) %>
	 <%= link_to("Show",:action => 'showEvents', :id => event.id) %>
	         
	      </td>
	    </tr>
	    <% end %>
	  </table>
	</div>

 </div>
</body>
</html>

